<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Featured Books</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .card {
      transition: transform 0.3s ease, box-shadow 0.3s ease, filter 0.3s ease;
      max-width: 250px;
      height: auto;
    }
    .card:hover {
      transform: scale(1.05);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
      filter: brightness(90%);
    }
    .card-body {
      padding: 1rem;
    }
    .card-img-top {
      height: 200px;
      object-fit: cover;
    }
    .card-title,
    .card-text {
      color: black;
    }
    .text-muted {
      font-size: 0.9rem;
    }
  </style>
</head>
<body class="bg-light">
  <section class="py-5">
    <div class="container">
      <h2 class="text-center mb-4">Featured Books</h2>
      <p class="text-center mb-5 text-muted">Discover Your Next Great Read</p>

      
<form action="/catalog" method="get" class="mb-5">
  <div class="input-group shadow rounded">
    <input type="text" class="form-control border-0 ps-3" name="search" placeholder="Search by Book Name, Author, or ISBN" value="<%= searchQuery %>" style="border-top-left-radius: 25px; border-bottom-left-radius: 25px;">
    <button class="btn btn-dark px-4" type="submit" style="border-top-right-radius: 25px; border-bottom-right-radius: 25px;">Search</button>
  </div>
</form>


      <div class="row g-4">
        <% books.forEach(book => { %>
          <div class="col-md-4 col-lg-3">
            <a href="/book/<%= book.id %>" class="text-decoration-none">
              <div class="card shadow-sm rounded h-100">
                <img src="<%= book.cover %>" alt="<%= book.title %>" class="card-img-top">
                <div class="card-body">
                  <p class="card-title"><strong>Book Name:</strong> <%= book.title %></p>
                  <p class="card-text"><strong>Author:</strong> <%= book.author %></p>
                  <p class="card-text"><strong>ISBN:</strong> <%= book.ISBN %></p>
                </div>
              </div>
            </a>
          </div>
        <% }); %>

        <% if (books.length === 0) { %>
          <div class="col-12 text-center">
            <p>No books found for your search criteria.</p>
          </div>
        <% } %>
      </div>
    </div>
  </section>

</body>
</html>
