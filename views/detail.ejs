<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= book.title %> - Book Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-secondary-subtle">


<section class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
                <div class="row g-0">
                   
                    <div class="col-md-6">
                        <img src="<%= book.cover %>" class="img-fluid rounded-start w-100 h-100 object-fit-cover" alt="<%= book.title %>">
                    </div>
                   
                    <div class="col-md-6">
                        <div class="card-body p-4">
                            <h3 class="card-title text-dark mb-3"><%= book.title %></h3>
                            <p class="fw-bold mb-1">Author: <span class="fw-normal"><%= book.author %></span></p>
                            <p class="fw-bold mb-1">Genre: <span class="fw-normal"><%= book.genre %></span></p>
                            <p class="fw-bold mb-1">Publisher: <span class="fw-normal"><%= book.publisher %></span></p>
                            <p class="fw-bold mb-1">ISBN: <span class="fw-normal"><%= book.ISBN %></span></p>
                            <h5 class="mt-4 text-dark">Book Story:</h5>
                            <p class="text-muted"><%= book.description %></p>
                        </div>
                    </div>
                </div>
                
                <div class="text-center mt-4 mb-4">
                    <a href="/catalog" class="btn btn-dark px-4 py-2 rounded-pill shadow-sm">Back to Catalog</a>
                </div>
            </div>
        </div>
    </div>
</section>

</body>
</html>
